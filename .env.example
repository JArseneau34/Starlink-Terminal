# SPCX API Server — copy to .env and fill in values

PORT=3002

# Space-Industry-Data-Pipeline FastAPI (fallback when local sat-stats snapshot unavailable;
# required for Investor → Global Catalog tab at /global/*)
PIPELINE_API_URL=http://localhost:8000
# PIPELINE_CACHE_TTL_MS=600000

# ── Sat Stats (local aggregation module) ───────────────────────────────────
# SAT_STATS_DB_PATH=.cache/sat-stats/sat_stats.db
# SAT_STATS_LANDING_ZONE=.cache/sat-stats/landing
# SAT_STATS_EXPORTS_DIR=.cache/sat-stats/exports
# SAT_STATS_LAYER1_DIR=.cache/sat-stats/layer1_raw
# SAT_STATS_FETCH_TIMEOUT_MS=45000

# Space-Track (optional — verify/catalog only; credentials from https://www.space-track.org)
SPACETRACK_USER=
SPACETRACK_PASSWORD=

# Launch Library 2 — optional Patreon key from https://thespacedevs.com/supportus
# LL2_API_URL=https://ll.thespacedevs.com/2.3.0
LL2_API_KEY=

# FCC ECFS search terms (comma-separated)
# FCC_SEARCH_TERMS=Starlink,SpaceX constellation

# McDowell Starlink stats page
# MCDOWELL_STARLINK_STATS_URL=https://planet4589.org/space/con/star/stats.html

# Finnhub — free at https://finnhub.io/register (60 REST/min + WebSocket trades)
FINNHUB_API_KEY=

# Marketaux — optional, free at https://www.marketaux.com (sentiment + entity tagging)
MARKETAUX_API_KEY=

LL2_USE_DEV=false
NASA_API_KEY=DEMO_KEY

# ── Quote speed (all free except Finnhub key) ─────────────────────────────
# Yahoo batch: all symbols in 1–2 requests, no key required
YAHOO_BATCH_MS=4000

# Finnhub REST: only polls symbols stale > FINNHUB_STALE_MS (saves API budget)
TURBO_POLL_MS=1200
FINNHUB_STALE_MS=20000
FINNHUB_REST_BUDGET_PER_MIN=45

# Client polls server memory (no external API cost)
TURBO_CLIENT_POLL_MS=250

# Cache TTLs (ms) — quotes cache is fallback; live stream is in-memory
CACHE_TTL_QUOTES=15000
CACHE_TTL_LAUNCHES=300000
CACHE_TTL_NEWS=180000
CACHE_TTL_TERMINAL=20000

# Starlink CelesTrak TLE cache (disk + memory). Default 8 hours.
# STARLINK_TLE_CACHE_TTL_MS=28800000
# STARLINK_TLE_CACHE_PATH=.cache/starlink/tle.json

# Node TLS — use OS certificate store (required for TLE API HTTPS on Windows)
NODE_OPTIONS=--use-system-ca

# News wire — merged headline cap and per-source fetch limits
NEWS_MERGE_LIMIT=100
NEWS_SNAPI_SITES_LIMIT=40
NEWS_MARKETAUX_LIMIT=50
NEWS_FINNHUB_COMPANY_LIMIT=8
